{
  "name": "spi-opponent-scouting-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend package for SPI Opponent Scouting API.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test --import tsx tests/**/*.test.ts"
  },
  "dependencies": {
    "fastify": "^5.3.0",
    "mysql2": "^3.15.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
